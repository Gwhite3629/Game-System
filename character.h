#ifndef _CHARACTER_H_
#define _CHARACTER_H_

#include "types.h"

err_t init_char (Character **character);

err_t write_char (Character character);

err_t create_char(Character character);

#endif // _CHARACTER_H_